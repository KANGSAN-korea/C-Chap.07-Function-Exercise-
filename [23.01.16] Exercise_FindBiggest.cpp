#include <iostream>

int main() {

}

// 함수의 길이와 끝을 어떻게 받아??, 그리고 반환 형식은 배열로 잡아야해?
int replace(char* str, char c1, char c2) {
	char arr[0] = str;
	int count;
	if (int i = 0; i < int index; index++) {
		if (arr[i] != c1) {
			arr[i] = c2
				count++;
		}
	}
}

int replace(char* str, char c1, char c2) {
	int count = 0;
	while (*str) {
		if(*str == c1)
	}
}